- defaults:
    link:
      relink: true
      create: true
- clean: ['~']
- link:
    ~/.vimrc: vim/vimrc
    ~/.vim: vim/vim
    ~/.gitconfig: git/gitconfig
- shell:
  - [git submodule update --init --recursive vim/plugins]
