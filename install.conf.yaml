- clean: ['~']
- link:
    ~/.config/alot: alot/
    ~/.config/alot/config: alot/config.private
    ~/.gvimrc: gvimrc
    ~/.lbdbrc: lbdbrc
    ~/.lftprc: lftprc
    ~/.mailcap: mailcap
    ~/.msmtprc: msmtprc.private
    ~/.mutt: mutt/
    ~/.muttrc: muttrc
    ~/.muttrc.aliases: muttrc.aliases
    ~/.muttrc.private: muttrc.private
    ~/.notmuch-config: notmuch-config
    ~/.offlineimap: offlineimap.private/
    ~/.offlineimaprc: offlineimaprc.private
    ~/.signature: signature
    ~/.signature.ascm: signature.ascm
    ~/.task: task.private/
    ~/.taskrc: taskrc
    ~/.tigrc: tigrc
    ~/.timewarrior/timewarrior.cfg: timewarrior.cfg
    ~/.vim: vim
    ~/.vimrc: vimrc
    ~/.zshenv: zshenv
    ~/.zshrc: zshrc
    ~/.zshrc.private: zshrc.private
- shell: []
